# Example configuration. Rename to config.yaml or use environment variables.
api:
  addr: ":8080"

worker:
  # queue: memory | rabbitmq | sqs
  queue: memory

# Uncomment when using RabbitMQ or other plugins
# rabbitmq:
#   url: "amqp://guest:guest@rabbitmq:5672/"
#   queue: "tasks"
